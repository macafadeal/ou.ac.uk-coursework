#This template has been created to accompany the Week-19 Iteration work on the VCE
#Note: The code in this template will not run without replacing the values surrounded by chevrons for
#      more appropriate values. You can use the material in the VCE for examples on how to do this.
#  This script can be executed using 'python3 ./skeletons/week_19_iteration-skeleton.py' in the console


fruits = [<many elements>] # you will need to replace <many elements> with values
for fruit in fruits: # remember the colon
    #remember to indent code here


for count in range(<a limit value>): # Limit value should be replaced with a number
    #remember to indent code

count = <suitable initialisation value>  # use a suitable initialisation value (0)
while count <limit value> :  #use a sensible limit value
    count += <increment value>. #use a suitable increment value
